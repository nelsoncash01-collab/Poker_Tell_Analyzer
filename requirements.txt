# Core data/stats dependencies used by the sync layer and (later) modeling.
numpy>=1.24
pandas>=2.0

# Video ingestion: PyAV reads true per-frame PTS (handles VFR/inaccurate-fps
# broadcast footage). Wheels bundle ffmpeg, so no system ffmpeg is required.
av>=12.0

# Footage acquisition: download source video from YouTube etc. For best-quality
# merged MP4s, also install the ffmpeg *binary* on your machine (separate from
# PyAV's bundled libs): brew/apt install ffmpeg, or https://ffmpeg.org.
yt-dlp>=2024.1.0

# Broadcast overlay OCR (reading pot/cards/actions from on-screen graphics).
# Overlays are *detected by appearance* (OpenCV); card recognition is numpy-only;
# the text step needs pytesseract + Pillow and the system tesseract binary
# (apt-get install -y tesseract-ocr).
opencv-python-headless>=4.8
pytesseract>=0.3.10
Pillow>=10.0

# Testing
pytest>=7.4

# --- Not yet used; uncomment as the corresponding pipeline stage is built. ---
# Computer vision (feature extraction):
# opencv-python>=4.8
# mediapipe>=0.10
# py-feat>=0.6            # FACS action units
# ultralytics>=8.0        # YOLOv8 (chips/cards/overlay detection)
# Audio (only if usable table-talk audio exists):
# openai-whisper
# librosa>=0.10
# Modeling:
# scikit-learn>=1.3
# scipy>=1.11
